import React from 'react';
import SearchResultContainer from './SearchResultContainer';

const Main = (props) => {

    return ( <
        div className = "Main" >
        <
        SearchResultContainer / >
        <
        /div>
    );
}

export default Main;